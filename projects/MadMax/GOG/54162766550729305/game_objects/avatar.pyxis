use game_objects::character::Character;
use types::shared_ptr::WeakPtr;

backend rust prologue "use crate::{math::Matrix4, shared_ptr::*};";

#[size(0x58), align(8)]
pub type Avatar {
    vftable {
        #[index(6)]
        pub fn set_opacity(&mut self, val: f32);
    },

    #[address(0x10)]
    pub character: *mut Character,
    pub weak_this: WeakPtr<Avatar>,
}
