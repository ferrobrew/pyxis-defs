use game_objects::game_object::GameObject;
use types::shared_ptr::WeakPtr;

backend rust prologue "use crate::{GameObject, WeakPtr};";

#[singleton(0x1_191_720)]
pub type GameWorld {
    vftable {
        pub fn destructor(&mut self);

        #[index(2)]
        pub fn add_object(&mut self, vehicle: WeakPtr<GameObject>);
        pub fn remove_object(&mut self, vehicle: WeakPtr<GameObject>);
    },
}
