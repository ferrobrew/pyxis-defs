use game_objects::character::Character;
use types::shared_ptr::{SharedPtr, WeakPtr};

#[size(0x70)]
pub type Avatar {
    vftable {
        pub fn destructor(&mut self);
    },

    #[address(0x44)]
    pub character: SharedPtr<Character>,
    pub weak_this: WeakPtr<Avatar>,
}
