use types::{std_string::StdString, shared_ptr::SharedPtr};

backend rust prologue "use crate::shared_ptr::*;";

#[singleton(0x1_190_B50)]
pub type TextureManager {
    vftable {
        pub fn destructor(&mut self);

        #[index(10)]
        pub fn get_texture(&mut self, texture: *mut SharedPtr<u32>, path: *const StdString, format: u32, lods: u32, mips: u32) -> *mut SharedPtr<u32>;
    },
}
